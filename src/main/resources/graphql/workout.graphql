type WorkoutSession {
    creationTimestamp: Timestamp
    updateTimestamp: Timestamp
    id: ID
    user: User
}

type WorkoutSessionPage {
    content: [WorkoutSession]
    totalElements: Int
    totalPages: Int
    number: Int
    size: Int
}
